package com.okmich.common.entity.criteria;

/**
 * Defines the two basic way to perform order by in a query
 * @author m-enudi
 */
public enum OrderType {
    /**
     * represents an order by ascending order
     */
    ASC, 
    /**
     * represents an order by descending order
     */
    DESC
}
